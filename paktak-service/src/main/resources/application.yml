server:
  port: 8081
spring:
  application:
    name: "paktak-service"
  boot:
    admin:
      client:
        url: "http://localhost:8088"
management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
     defaultZone:  "http://localhost:8000/eureka/"